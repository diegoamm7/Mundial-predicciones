# Copiá este archivo a `.env` y completá con tus valores reales.
# Estos valores se inyectan directamente en src/js/supabase.js (no hay build step).

SUPABASE_URL=https://tu-proyecto.supabase.co
SUPABASE_ANON_KEY=eyJhbGciOi...

# Opcional — para push notifications con VAPID
VAPID_PUBLIC_KEY=
